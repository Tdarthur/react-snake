import React from 'react';
import * as directions from '../directions';

const SnakeSegment = ({ startDirection, endDirection }) => {
    return <div className='snake-segment'></div>;
};

export default SnakeSegment;
